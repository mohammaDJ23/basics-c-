/*

  += => lhs += rhs => lhs = lhs + (rhs);
  -= => lhs -= rhs => lhs = lhs - (rhs);
  *= => lhs *= rhs => lhs = lhs * (rhs);
  /= => lhs /= rhs => lhs = lhs / (rhs);
  %= => lhs %= rhs => lhs = lhs % (rhs);
  >>= => lhs >>= rhs => lhs = lhs >> (rhs);
  <<= => lhs <<= rhs => lhs = lhs << (rhs);
  &= => lhs &= rhs => lhs = lhs & (rhs);
  ^= => lhs ^= rhs => lhs = lhs ^ (rhs);
  |= => lhs |= rhs => lhs = lhs | (rhs);

*/

#include <iostream>

int main()
{
  return 0;
}